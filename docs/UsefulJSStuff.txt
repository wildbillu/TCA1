/*
// https://wildbillu.github.io/TCA.github.io/?file=C.txt&device=P&
// https://wildbillu.github.io/TCA.github.io/?file=C.txt&device=D&
hiding keyboard
- making things readonly but then....

const field = document.createElement("input");
field.setAttribute("type", "text");
document.body.appendChild(field);

setTimeout(() => {
  field.focus();
  setTimeout(() => {
    field.setAttribute("style", "display: none;");
  }, 50);
}, 50);
*/    
//Window.getComputedStyle(elem)

elem = document.getElementById("AllTable");
var ss = window.getComputedStyle(elem)
    alert('ss:' + ss)
    alert(ss.getPropertyValue('height'))
    alert(ss.getPropertyValue('width'))

/*    
// finding android
    var ua = navigator.userAgent.toLowerCase();
    var g_bIsAndroid = ua.indexOf("android") > -1;
    elem = document.getElementById("AllTable");
    if (g_bIsAndroid) 
    {
        alert('Android')
        alert('elem:' + elem)
        elem.className = 'AllTable_Android'
    }
    alert('class:' + elem.className)
    elem = document.getElementById("AllTable");
    if (elem.requestFullscreen) 
    {
        elem.requestFullscreen();
        alert('fs')
    } 
    if (elem.webkitRequestFullscreen) 
    { // Safari
        elem.webkitRequestFullscreen();
        alert('sf')
    }
    if (elem.msRequestFullscreen) 
    { // IE11
        alert('ie')
        elem.msRequestFullscreen();
    }
//    
    if ( iPairs != 0 )
    {
        for ( iPair = 0; iPair < aPairs.length; iPair++ )
        {
            var sDevice = '';
            var iFound = aPairs[iPair].indexOf("device=");
            if ( iFound != -1 )
            {
               sDevice = aPairs[iPair].substring(iFound+7);
                if ( sDevice == 'D' )
                {
                    elem = document.getElementById("All");
                    if (elem.requestFullscreen) 
                    {
                        elem.requestFullscreen();
                    } 
                    else if (elem.webkitRequestFullscreen) 
                    { // Safari
                        elem.webkitRequestFullscreen();
                    } else if (elem.msRequestFullscreen) 
                    { // IE11 
                        elem.msRequestFullscreen();
                    }
                }
            }
        }
    }
*/    
